
event_inherited()

function cost_info_text(type) {
	
}

self.add_item(-1, resource_cost_text("metall"), new Producer("metall", id))
self.add_item(-1, resource_cost_text("fuel"), new Producer("fuel", id))
self.add_item(-1, resource_cost_text("part"), new Producer("part", id))
self.add_item(-1, resource_cost_text("repair_kit"), new Producer("repair_kit", id))
self.add_item(-1, resource_cost_text("bullet_homing"), new Producer("bullet_homing", id))
self.add_item(-1, resource_cost_text("drives"), new Producer("drives", id))
//self.add_item(-1, resource_cost_text("chip"), new Producer("chipw", id))

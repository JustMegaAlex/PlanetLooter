
draw_sprite_ext(sprite_index, image_index, x, y, 1, 1, dir, c_white, 1)

//draw_text(x, y - 40, id)
var xx = x + battle_strafe_vec.X * 100
var yy = y + battle_strafe_vec.Y * 100
//draw_line(x, y, xx, yy)
//draw_text(x, y - 20, "sp to: " + string(hsp_to) + " " + string(vsp_to))

//if is_patrol {
//	var p = patrol_point_to
//	draw_line(x, y, p.X, p.Y)
//}
